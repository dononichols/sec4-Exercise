
// Exercise ___ vid.10
// Max of Two Numbers

// Write a function that takes two numbers and returns the maximum of the two.

let number = max(6, 5);
console.log(number);

function max(a, b) {
    // if (a > b) return a;
    // return b;

    // or write it like this
    return (a>b) ? a : b;
}